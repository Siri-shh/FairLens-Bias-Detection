# FairLens — Unified Backend Dependencies
# Install with: pip install -r requirements.txt

# ── Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.6
python-dotenv>=1.0.1
pydantic>=2.7.1
slowapi>=0.1.9

# ── Data tools
pandas>=2.2.2
numpy>=1.26.4
joblib
scikit-learn>=1.5.0

# ── Fairness libraries
aif360>=0.6.1
fairlearn>=0.10.0

# ── SHAP
shap>=0.45.1

# ── PDF generation
weasyprint>=62.3
Jinja2>=3.1.3

# ── PDF fallback (used in pdf_generator.py when WeasyPrint unavailable)
reportlab>=4.0.0

# ── Gemini — FairLens uses the Google AI Studio REST API directly (urllib).
# No GCP SDK needed. The GEMINI_API_KEY env var is sufficient.
# See: backend/services/gemini.py → _BASE = generativelanguage.googleapis.com

# ── Caching (in-memory TTL cache, no Redis/external service needed)
cachetools>=5.3.0

# ── ONNX (optional — only needed for .onnx model inference)
# onnxruntime
